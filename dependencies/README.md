# Dependencies

Environment files, Dockerfiles, and external dependency notes for training.

Integration-specific training dependencies live with their integration adapter.
For the Sketchformer codebase fine-tuning Docker environment, see
`integrations/original_sketchformer/docker/`.
